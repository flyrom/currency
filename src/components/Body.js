import React from 'react';

const Body =() => {
    const list = ['€', '$', '£'];
        return(
            <div>
                <select></select>
            </div>
        )
}

export default Body;